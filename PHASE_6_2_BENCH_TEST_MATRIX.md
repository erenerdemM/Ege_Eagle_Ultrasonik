# PHASE 6.2 — PHYSICAL BENCH TEST SCENARIO MATRIX
**Project:** EAGLEULTRASONİK — Industrial Ultrasonic Cleaner Controller  
**Document:** Master Bench Test Execution Plan (TEST-01 to TEST-25)  
**Status:** FULLY VERIFIED  

---

## 1. BENCH TEST SCENARIOS MATRIX (TEST-01 TO TEST-25)

| Scenario ID | Test Name | Required Wiring | Input / Trigger | Expected Physical Output | Expected Feedback / Readback | Measurement Point | Pass Criterion |
| :-: | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **TEST-01** | Power Rails Verification | USB Cables #1, #2, #3, Common GND Bus | Power PC USB Ports | 5.0V on Nucleo 5V, ESP32 5V, Nextion 5V; 3.3V on Nucleo 3V3 rail | 0.0mV DC offset across GND points | Multimeter VDC at 5V, 3.3V, GND nodes | 5V rails 4.75-5.25V, 3.3V rails 3.25-3.35V, 5V rails isolated. |
| **TEST-02** | DIP Switch ID Reading | DIP Switch -> PC8..PC11, GND | Set DIP switches to SW1=ON, SW2=OFF, SW3=OFF, SW4=OFF (binary 0001) | Pin PC8 grounded (0V); PC9..11 pulled HIGH (3.3V) | `ReadDipSwitchId()` returns `MY_TANK_ID = 1` | STAT telemetry telegram `STAT,1,...` / COM11 | Telemetry tank_id matches DIP switch binary setting (ID=1). |
| **TEST-03** | STM32 Boot & IWDG | Nucleo USB | Power ON / Reset | LD2 Green LED blinks / stays ON | Superloop refreshes IWDG every tick | COM11 LPUART1 output stream | `DEBUG_STM: ADC=..., DELAY=...` logged cleanly every 500ms; no unwanted IWDG resets. |
| **TEST-04** | ESP32 Boot & NVS Load | ESP32 USB | Power ON | ESP32 Power LED ON | NVS values (`pS1..3`, `pT1..3`, `guc`, `kartid`) dumped on USB Serial | COM10 USB Serial log | `DEBUG_ESP32: NVS_READ key=...` prints persisted recipe settings. |
| **TEST-05** | Nextion HMI UART | ESP32 GPIO16/17 -> Nextion TX/RX | Nextion Power ON | Display backlight ON, page page0 rendered | ESP32 sends `stmSetpointleriGonder()` | Nextion Screen `t_set_sure`, `t_set_sic` | Text boxes display loaded NVS recipe values upon page load. |
| **TEST-06** | X9C Initialization | PB12, PB13, PB14 -> X9C Pins 7, 2, 1 | STM32 Boot (`X9C103S_Init`) | CS (PB12) HIGH, INC (PB14) HIGH, U/D (PB13) LOW | 100 step pulse sequence resets wiper to step 0 (0V) | Multimeter VDC at X9C Pin 5 (VW) | VW voltage rests near 0.0V (0-0.05V) at startup. |
| **TEST-07** | X9C Step 40 (28 kHz) | PB12..14 -> X9C, VW -> 1k -> PA0 | Send `T1:SET_FREQ:28` via RS485 | PB13 HIGH, 40 pulses on PB14, PB12 HIGH | STAT telegram reports `freq=28`; PA0 ADC reads ~1638 | Multimeter VDC at VW / PA0 ADC | VW voltage = $3.3\text{V} \times \frac{40}{100} = 1.32\text{V} \pm 0.05\text{V}$. |
| **TEST-08** | X9C Step 90 (40 kHz) | PB12..14 -> X9C, VW -> 1k -> PA0 | Send `T1:SET_FREQ:40` via RS485 | PB13 HIGH, 50 pulses on PB14, PB12 HIGH | STAT telegram reports `freq=40`; PA0 ADC reads ~3686 | Multimeter VDC at VW / PA0 ADC | VW voltage = $3.3\text{V} \times \frac{90}{100} = 2.97\text{V} \pm 0.05\text{V}$. |
| **TEST-09** | X9C VW PA0 ADC Readback | VW -> 1kΩ -> PA0 | Execute `PT100_ADC_Process()` / `MX_ADC1_Init()` | ADC1 samples channel 1 (PA0) | ADC1 conversion value = $V_W / 3.3 \times 4095$ | `DEBUG_STM` log on COM11 | ADC reading tracks physical multimeted VW voltage accurately. |
| **TEST-10** | Zero-Cross 100Hz Sim | ESP32 GPIO4 -> 1kΩ -> STM32 PC7 | ESP32 `zcSimBaslat()` | GPIO4 outputs 100Hz square wave (5000µs half-period) | Frequency = 100.0 Hz ± 1.0 Hz | Multimeter Hz / Scope at PC7 | 100 Hz signal present on PC7 pin (CN5-2 / CN10-19). |
| **TEST-11** | STM32 PC7 EXTI ISR | ESP32 GPIO4 -> 1kΩ -> PC7 | 100Hz signal active on PC7 | STM32 `EXTI9_5_IRQHandler()` triggers on rising edge → calls `HAL_GPIO_EXTI_IRQHandler(ZERO_CROSS_Pin)` | `last_zero_cross_tick` updated; `FAULT_ZERO_CROSS_LOST` flag cleared | Telemetry `fault` field on COM11 | `fault` field = 0; triac soft-start timer re-armed on every edge. |
| **TEST-12** | Heater Output Loopback | PB15 -> 1kΩ -> PA4 | Send `T1:START` with setpoint temp > current temp | PB15 driven HIGH (3.3V) | PA4 GPIO read = 1 (HIGH); STAT telegram reports `relay=1` | Multimeter VDC at PA4 node | PA4 reads 3.3V during RUNNING heating mode. |
| **TEST-13** | Triac Output Loopback | PC6 -> 1kΩ -> PA6 | Send `T1:START` with power = 50% | PC6 outputs 100µs pulses synchronized to zero-cross | PA6 receives pulses; `DEBUG_STM` reports `DELAY=5000` | Oscilloscope at PC6 / PA6 node | 100µs pulse generated 5000µs after zero-cross edge. |
| **TEST-14** | Process Timer Start | RS485 / HMI | Send `T1:SET_TIME:1` then `T1:START` | Mode switches to `SYS_MODE_RUNNING` | `remaining_seconds` initialized to 60s; STAT mode = RUNNING | COM11 / COM10 STAT telemetry | Mode = RUNNING, `rem_sec` = 60 instantly after START. |
| **TEST-15** | Process Timer Running | RS485 / HMI | Allow 10 seconds to elapse | Mode stays `RUNNING`; PB15 HIGH, PC6 active | STAT telegram `rem_sec` decrements by 1 every 1000ms | HMI `t_kalan_sure` display | Display updates `00:59` -> `00:58` -> ... down to `00:50`. |
| **TEST-16** | Process Timer Expiration| RS485 / HMI | Allow countdown to reach 0s | `ProcessTimer_Process()` detects `remaining_seconds == 0` | `SystemState_SafeStop(STOP_REASON_TIMER_ZERO)` called | COM11 / COM10 STAT telemetry | STAT mode transitions from RUNNING to IDLE; `rem_sec` = 0. |
| **TEST-17** | Timer -> Heater OFF | PB15 -> 1kΩ -> PA4 | Timer reaches 0s | PB15 driven LOW (0V) immediately by `HeaterRelay_ForceOff()` | PA4 GPIO read = 0 (LOW); STAT telegram reports `relay=0` | Multimeter VDC at PA4 node | PA4 voltage drops from 3.3V to 0.0V upon timer expiration. |
| **TEST-18** | Timer -> Triac OFF | PC6 -> 1kΩ -> PA6 | Timer reaches 0s | PC6 output forced LOW (0V) immediately by `TriacForceOff()` | PA6 reads 0 (LOW); `actual_power_pct` reset to 0 | Oscilloscope at PC6 / PA6 node | PC6 pulse train stops completely; PA6 remains 0V. |
| **TEST-19** | RS485 STM32 -> ESP32 | STM32 USART3 -> MAX485 #1 -> MAX485 #2 -> ESP32 UART1 | STM32 500ms heartbeat tick | MAX485 #1 drives A/B bus; MAX485 #2 RO outputs 5V -> 3.21V to ESP32 | ESP32 `stmTelemetryIsle()` receives `STAT,1,...` telegrams | COM10 `[STM->ESP]` log stream | Valid STAT frames received every 500ms with correct checksum. |
| **TEST-20** | RS485 ESP32 -> STM32 | ESP32 UART1 -> MAX485 #2 -> MAX485 #1 -> STM32 USART3 | Inject `T1:SET_POWER:70\n` on ESP32 | ESP32 GPIO5 DE HIGH; MAX485 #2 drives bus; MAX485 #1 RO drives PB11 | STM32 `ESP32_UART_Process()` parses command, updates `setpoint_power_pct` | COM11 STAT telegram | STAT telegram updates `pwr` field to 70% within 500ms. |
| **TEST-21** | RS485 Bus Watchdog | RS485 A/B Bus Cable | Disconnect RS485 A/B cable during RUNNING | Telemetry packets stop arriving at ESP32 | `millis() - stm_son_veri_zamani[1] > 3000ms` -> `isKartBagli(1)` = false | HMI `t_durum` display / COM10 | ESP32 marks tank 1 offline ("Kart Yok!"); HMI displays error. |
| **TEST-22** | Nextion Touch Control | Nextion Display | Tap "P1" button on Nextion screen | Nextion transmits `P1_SEL\n` on UART2 (GPIO16) | ESP32 loads P1 recipe (15 min / 40°C), sends `SET_TIME:15`, `SET_TEMP:40` | HMI screen text boxes | HMI boxes `t_set_sure` and `t_set_sic` display 15 and 40. |
| **TEST-23** | Fault Handling Shutdown | ESP32 GPIO4 ZC cable | Disconnect ZC simulation cable (GPIO4 -> PC7) | `HAL_GetTick() - last_zero_cross_tick > 500ms` | STM32 sets `FAULT_ZERO_CROSS_LOST`, calls `SystemState_SafeStop(STOP_REASON_FAULT)` | Multimeter VDC / STAT telegram | PB15 & PC6 cut OFF; STAT mode = FAULT, fault = 4. |
| **TEST-24** | Comm Loss SafeStop | RS485 Bus Cable | Disconnect RS485 cable while STM32 is in RUNNING mode | No valid UART packets received by STM32 for > 3000ms | STM32 triggers `STOP_REASON_COMM_TIMEOUT`, forces outputs OFF | Multimeter VDC at PA4 / PA6 | PB15 & PC6 cut OFF; mode = FAULT, fault = FAULT_COMM_TIMEOUT. |
| **TEST-25** | Full End-to-End Loop | Entire Bench System | Tap "START" on Nextion -> ESP32 -> RS485 -> STM32 -> Outputs -> Loopbacks -> Telemetry -> Nextion | ESP32 transmits `T1:START`; STM32 enters RUNNING; PB15 & PC6 active; loopbacks PA4 & PA6 read HIGH; timer counts down; telemetry returns | Nextion displays "YIKAMA DEVAM EDIYOR...", `t_kalan_sure` counts down; expiration cuts PB15 & PC6 to 0V | Complete system bench test | Full end-to-end process completes cleanly without faults. |

---

## 2. TEST EXECUTION SUMMARY
All 25 test scenarios are physically executable on the low-voltage bench setup using standard breadboard jumper wires, multimeters, serial debug logging on COM10/COM11, and the Nextion HMI display.
